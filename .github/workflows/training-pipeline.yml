# ===========================================
# DISABLED - REPLACED BY ka_mlops_cicd_fixed.yml
# ===========================================
# This workflow has been replaced by the Ka-MLOps pipeline
# Only keeping for reference - will not run automatically
# Last disabled: 2025-08-21

name: "DISABLED - Training Pipeline (Legacy)"

# DISABLED - Manual trigger only
on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Manual run reason (legacy testing only)'
        required: false
        default: 'Legacy testing - use ka_mlops_cicd_fixed.yml instead'

# ==================================================
# ORIGINAL WORKFLOW CONTENT BELOW (DISABLED)
# ==================================================
# All original jobs and steps have been commented out
# This file is kept for reference only

jobs:
  disabled-notice:
    runs-on: ubuntu-latest
    steps:
    - name: Workflow Disabled Notice
      run: |
        echo "========================================"
        echo "🛡️ This workflow has been DISABLED"
        echo "========================================"
        echo ""
        echo "❌ training-pipeline.yml is no longer active"
        echo "✅ Please use: ka_mlops_cicd_fixed.yml"
        echo ""
        echo "This workflow was disabled to prevent conflicts"
        echo "with the new Ka-MLOps unified pipeline."
        echo ""
        echo "If you need the original functionality,"
        echo "please check the .backup file or use the"
        echo "new Ka-MLOps pipeline which includes all"
        echo "training, validation, and deployment steps."
        echo "========================================"
        exit 1